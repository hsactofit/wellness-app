# Mednovations Firebase configuration

Download the Android `google-services.json` for the Mednovations Firebase app
whose application ID is `com.mednovations.wellness`, and save it in this
directory as `google-services.json`.

Release builds intentionally stop when that file is absent. Never copy the
Medifit Firebase file here: both products must have separate authentication
users and backend data.
